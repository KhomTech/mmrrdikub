# 🚀 คัมภีร์มหาเวทย์ MMRRDIKUB (ฉบับเจาะลึกทะลวงไส้ ภาษาบ้านๆ) 🚀

สวัสดีครับยอดนักพัฒนา! ไฟล์นี้คือ **"คัมภีร์หลัก"** (Master Guide) ที่รวบรวมทุกอณูของโปรเจกต์ **MMRRDikub** เอาไว้ที่นี่ที่เดียว อธิบายแบบ "ภาษาบ้านๆ" เผื่อเอาไว้อ่านทบทวนตัวเอง หรือเอาไปตอบคำถามตอนพรีเซนต์โปรเจกต์ รับรองว่าอ่านจบปุ๊บ บรรลุธรรม เข้าใจตั้งแต่ 0 - 100 แน่นอน! 🔥

---

## 🎯 1. โปรเจกต์นี้คืออะไร? ทำไปทำไม? (Introduction)
**MMRRDikub (Risk Management + AI for Traders)** เกิดมาเพื่อแก้ปัญหา "นักเทรดมือใหม่เจ๊งเพราะคุมความเสี่ยงไม่เป็น"
ปกติคนเทรดจะใช้อารมณ์ แต่แอปเราจะบังคับให้ใช้ "สติ" โดยมี:
- 🧮 **เครื่องคิดเลขคำนวณความเสี่ยง (Position Size Calculator):** บอกเลยว่าถ้าผิดทางจะเสียกี่บาท ต้องซื้อเหรียญจำนวนเท่าไหร่
- 🤖 **AI Risk Analyst (หมอดู AI ประจำพอร์ต):** ตรวจสอบแผนการเทรดก่อนกดซื้อจริง แนะนำว่าจุดเข้า-จุดออกนี้เสี่ยงไปไหม
- 📰 **RAG Engine (ระบบดึงข่าวจริงเรียลไทม์):** ดึงข่าวคริปโตล่าสุดมายัดใส่สมอง AI ให้มันรู้อนาคตตลาด ไม่ใช่แค่มั่วแหลก
- 🌍 **ระบบหลายภาษา (i18n):** รองรับ ไทย, อังกฤษ, จีน
- 🌙 **Hacker/Pro Trader Theme:** หน้าตาดุดัน สีดำ-เขียวนีออน ให้ฟีลลิ่งแฮกเกอร์ระดับโลก

---

## 🏗️ 2. สถาปัตยกรรมระบบ (System Architecture)
โปรเจกต์นี้ใช้โครงสร้างแบบ **Modern Web App (Frontend แยก Backend ขาดจากกัน)**

1. **🖥️ Frontend (หน้าบ้าน):** `Next.js (React)` + `TailwindCSS`
   - หน้าตาเว็บที่ผู้ใช้กดเล่นได้
   - สื่อสารกับหน้าบ้านผ่าน API (JSON)
2. **⚙️ Backend (หลังบ้าน):** `Golang (Go Fiber)`
   - สมองกลของระบบ รับคำสั่งจากหน้าบ้าน ไปดึงข้อมูลจาก Database และ AI
   - เร็วปรี๊ดดดด (Go ขึ้นชื่อเรื่องทำงานไวและกินแรมน้อยมาก)
3. **🗄️ Database (ฐานข้อมูล):** `PostgreSQL (Neon.tech)`
   - เก็บข้อมูลถาวร เช่น รหัสผ่านผู้ใช้ (เข้ารหัสแล้ว), ประวัติการเทรด
4. **🧠 AI & APIs (ผู้ช่วยตัวโกง):**
   - **Google Gemini API:** AI หลักที่ฉลาดมาก ไว้ใช้ตอบคำถาม
   - **Pollinations.ai (OpenAI Proxy):** AI ตัวสำรอง (Fallback) เผื่อ Gemini โควต้าหมด
   - **CryptoPanic API:** ไว้ดึงข่าวคริปโตล่าสุด

---

## 🐳 3. ทำไมต้องใช้ Docker? (Docker & Deployment)
หลายคนงงว่า Docker คืออะไร? ทำไมชีวิตมันถึงวุ่นวายตอนเอาขึ้น `Render.com`? ผมขออธิบายแบบนี้ครับ...

### 📦 Docker คืออะไรในภาษาบ้านๆ?
คิดภาพว่าโปรแกรมของเราคือ **"รถยนต์"** 
- ถ้าเราขับในคอมพิวเตอร์ตัวเอง (Local) เรามีน้ำมัน มีถนน มีอะไหล่ครบ (ติดตั้ง Go, Node.js ไว้แล้ว) รถมันก็วิ่งฉิว!
- แต่พอเราจะเอารถคันนี้ไปวิ่งบนเซิฟเวอร์คนอื่น (เช่น Render.com) ปรากฏว่าที่นั่นถนนไม่เหมือนกัน ไม่มีน้ำมันแบบที่เราใช้ ผลคือ... **รถพัง รันไม่ได้! (Error)**

👉 **Docker คือ "ตู้คอนเทนเนอร์เวทมนต์"** 
ก่อนเราจะเอารถไปส่ง เราจับรถยัดใส่ตู้คอนเทนเนอร์นี้ พร้อมกับยัด "ถนนส่วนตัว" "น้ำมัน" "อะไหล่" เข้าไปในตู้ด้วยเลย! พอถึงปลายทาง (Render.com) เซิฟเวอร์มันไม่ต้องแคร์เลยว่าในตู้คืออะไร มันแค่เสียบปลั๊กให้ไฟเข้า แล้วโปรแกรมในตู้ก็รันได้ทันที!! 

### 📜 แกะรอย Dockerfile (คัมภีร์สร้างตู้คอนเทนเนอร์)
เปิดไฟล์ `backend/Dockerfile` ซิ! เราใช้เทคนิคระดับโปรที่เรียกว่า **"Multi-stage Build"** (สร้าง 2 ขยัก เพื่อให้เบาหวิว)

```dockerfile
# ---- STAGE 1: ฝ่ายช่างก่อสร้าง (Builder) ----
FROM golang:1.24-alpine AS builder  # เรียกช่างก่อสร้างชื่อ พี่ Go (เวอร์ชั่น 1.24) ที่ตัวเล็กชื่องามสกุล Alpine
RUN apk add --no-cache git          # ให้พี่ Go ไปซื้อเครื่องมือชื่อ Git มาด้วย
WORKDIR /app                        # สร้างโต๊ะทำงานชื่อ /app
COPY go.mod go.sum ./               # ก๊อปปี้ใบสั่งซื้ออะไหล่ (go.mod) ไปวางบนโต๊ะ
RUN go mod download                 # ให้พี่ Go โหลดอะไหล่จากเน็ตมากองไว้
COPY . .                            # ก๊อปปี้โค้ดทั้งหมด (รถยนต์ของเรา) มาวาง
RUN CGO_ENABLED=0 GOOS=linux go build -o main ./cmd/api/  # สั่งพี่ Go ประกอบรถยนต์ให้เป็นก้อนเหล็กก้อนเดียว (ไฟล์ชื่อ main)

# ---- STAGE 2: ฝ่ายขนส่ง (Runtime) ----
FROM alpine:3.19                    # เรียกรถบรรทุกคันจิ๋วชื่อ Alpine (ไม่เอาเครื่องมือช่างมาให้หนัก)
RUN apk --no-cache add ca-certificates tzdata  # ใส่ใบอนุญาตการขับขี่อินเตอร์เน็ต (HTTPS) และนาฬิกาให้รถบรรทุก
ENV TZ=Asia/Bangkok                 # ตั้งนาฬิกาเป็นเวลาประเทศไทย
WORKDIR /app                        # สร้างที่จอดรถ
COPY --from=builder /app/main .     # **จุดบรรลุธรรม!** วิ่งกลับไปเอาแค่ "รถยนต์ (main)" จากฝ่ายช่าง มายัดใส่รถบรรทุกคันจิ๋วนี้
CMD ["./main"]                      # พอรถถึง Render.com ปุ๊บ สั่งสตาร์ทรถยนต์ทันที!
```
**ทำไมต้อง 2 ขยัก?** ถ้าเราเอาตู้ของช่างก่อสร้าง (Stage 1) ส่งไป Render เลย ไฟล์มันจะใหญ่มาก (ประมาณ 500MB+ เพราะมีเครื่องมือช่างเต็มไปหมด) ค่าไฟเซิฟเวอร์จะแพง! แต่พอเราหยิบมาแค่ "สิ่งประดิษฐ์ (main)" ไปใส่กล่องเปล่า (Stage 2) ไฟล์จะเหลือแค่ 10-20MB! เล็กโคตร เบาโคตร โหลดเร็วปรื๊ด! นี่คือความเทพของ Golang + Docker ครับ!

---

## ⚙️ 4. เจาะไส้ใน Backend (Golang & Fiber)
ระบบหลังบ้านของเราคล่องตัวสุดๆ เพราะใช้แนวคิด **Clean Architecture** (แบ่งบ้านเป็นสัดส่วน ไม่มั่วซั่ว)

### 📂 โครงสร้างโฟลเดอร์ใน `/backend`
- `cmd/api/main.go` -> **"ประตูหน้าบ้าน"** เป็นจุดเริ่มรันโปรแกรม ผูก API โยน Request ไปให้แผนกต่างๆ (Handlers)
- `internal/handlers` -> **"พนักงานต้อนรับ"** คอยรับคำสั่งจาก User (เช่น รับรหัสผ่าน, รับแผนเทรด) แล้วไปตามแผนกอื่นมาทำงาน
- `pkg/database` -> **"คนเฝ้าห้องโกดัง"** คอยติดต่อกับ PostgreSQL นำข้อมูลไปเก็บ หรือไปค้นออกมา

### 🤖 ระบบ AI ขั้นเทพ (RAG Engine + Fallbacks)
ไฟล์ที่สำคัญที่สุดคือ `internal/handlers/ai.go` และ `chat.go`
เราไม่ได้แค่เรียก AI โง่ๆ แต่เราติดอาวุธให้มัน (RAG - Retrieval-Augmented Generation)

**วงจรการทำงานของ AI Risk Analyst (ตอน User กดประเมินแผนเทรด):**
1. รับคำสั่ง (`AnalyzeTrade`) จากหน้าบ้าน
2. ดึงประวัติการเทรดเก่าของ User ขาดทุน/กำไรเท่าไหร่จาก Database 
3. ยิงไปหา `CryptoPanic API` เพื่อกวาดข่าวสารเหรียญนั้นๆ ณ วินาทีนี้! 
   - *Trick: มีระบบ Cache ข่าวไว้ 24 ชม. เพื่อไม่ให้มันไปขอ API บ่อยจนเราติดลิมิต*
4. ปั้น "Prompt" สุดยาว (เอาแผนเทรดปัจจุบัน + ประวัติในอดีต + ข่าววันนี้ + ภาษาที่ User เลือก) ยัดใส่มือ AI! 
   - **ด่าน 1:** เรียกใช้ `Google Gemini` (ฉลาดสุด)
   - **ด่าน 2 (เซฟโซน):** ถ้าแต้มบุญ Gemini หมด (Quota Limit หรือโชคร้ายเซิฟล่ม) โค้ดจะวิ่งไปหา `Pollinations AI (OpenAI Proxy)` ให้ตอบแทน! แบบฟรีๆ ไม่ต้องง้อคีย์
   - **ด่าน 3 (ไม้ตายสุดท้่าย):** ถ้าด่าน 1 และ 2 เน็ตล่มติดบั๊กรวมกัน ระบบจะเตะเข้าโหมด `Fallback` (คำนวณสดด้วยโค้ดธรรมดา) เพื่อให้เว็บไม่มีทางหน้าขาวเจ๊ง 100%!

### 🔐 ระบบ Login & Token (Auth) -> `handlers/auth.go`
การที่ใครจะเดินเข้ามาใช้หลังบ้านเราได้ เขาต้องมี **บัตรผ่าน (JWT - JSON Web Token)**
1. **สมัครสมาชิก (Register):** รับรหัสผ่านมา -> เอาไปใส่เครื่องปั่นสับ (`bcrypt`) ให้เป็นภาษาต่างด้าว -> เซฟลง Database. (สมมุติว่าแฮกเกอร์ขโมย DB เราไปได้ ก็จะเห็นแต่ตัวอักษรมั่วๆ ขโมยรหัสไม่ได้ 100%)
2. **เข้าสู่ระบบ (Login):** ดูรหัสผ่านว่าตรงกับเครื่องปั่นไหม -> ถ้าตรง! แจกบัตรผ่าน `JWT Token` คืนไปให้ Frontend
3. **การป้องกัน (Middleware):** ทุกครั้งที่ Frontend จะมาดึงข้อมูลหรือถาม AI มันจะต้องแนบบัตรผ่าน (JWT) นี้มาด้วย หลังบ้านเราถึงจะยอมตอบ

---

## 🖥️ 5. เจาะไส้ใน Frontend (Next.js 14)
หน้าบ้านเราเน้น 3 คำ: **"ลื่นปื๊ด, ดุดัน, พูดได้หลายภาษา"**

### 📂 ไฟล์เจ๋งๆ ที่ควรโฟกัส
- `app/page.tsx` -> แผงควบคุม (Dashboard) กลาง วาง Grid Layout แบ่งเป็น 4 ช่องเท่ๆ (เครื่องคิดเลข, แชทบอท, ประวัติ, ข่าวสาร)
- `app/context/LanguageContext.tsx` -> **กระทรวงการต่างประเทศ!** เป็นคลังศัพท์ที่เก็บภาษา `ไทย` `English` `中文` เผื่อใครอยากเปลี่ยนภาษา แค่กดปุ่ม ระบบจะรีเฟรชคำทั่วทั้งแอปแบบไม่กระตุก (ไม่ต้องโหลดหน้าเว็บใหม่)
- `app/components/AiChat.tsx` -> **แชทบอทของจริง!**
  - ไม่ได้แค่คุยจบแล้วทิ้ง แต่มีการจำประวัติ (History) ไว้ใน Local State
  - ส่งทั้งของเก่า+ของใหม่ไปหา Backend เพื่อให้ AI มันฉลาดขึ้นทุกครั้งที่โต้ตอบ!
  - กล่องแชทเลื่อนลงอัตโนมัติ (Auto-scroll) เวลามีข้อความใหม่
- `app/components/Calculator.tsx` -> **เครื่องคำนวณหน้าเลือด!**
  - โค้ดตรงนี้ซับซ้อนสุด เพราะต้องผูกสูตร `(ทุน * %เสี่ยง) / ระยะ SL` 
  - แถมสามารถปรับ Leverage ได้ตามชีวิตสายซิ่ง และกดส่งข้อมูลบินไปทะลุหลังบ้านหา AI Analyst ได้ในคลิกเดียว

---

## 🚨 6. เคลียร์ปมในใจ (Q&A ตอบคำถามสายเดือด)
เตรียมตัวให้พร้อม ถ้ามีคนถาม โปรเจคเรารอดแน่นอน!

**Q1: เห้ย! เอา API KEY ไว้ในโค้ด ปลอดภัยเหรอวะ?**
> **A:** ไม่มีทางครับ! คีย์ลับทั้งหมด (Gemini, Database) ถูกซ่อนอยู่ในไฟล์ `.env` ซึ่งผมเพิ่งสั่งสอนไฟล์ `.gitignore` ให้เตะมันออกจากการขึ้น Github ทิ้งไปแล้ว! รหัสเข้าบ้านเราอยู่แค่ในเครื่อง Local กับในหน้าตั้งค่าลับของ Render.com เท่านั้น แฮกเกอร์ไม่มีวันเห็น

**Q2: อาการหน้าจอ Render ขึ้น Error ว่า "go.mod requires go >= 1.24.0 (running go 1.23.xx)" คืออะไร แก้ยังไง?**
> **A:** อาการโลกแตกของวงการโปรแกรมเมอร์เรียกว่า **"Version Conflict"** ครับ! 
> - ปัญหาคือ ไฟล์ใบสั่งของ (`go.mod`) ดันเผลอไปพิมพ์บอก Render ว่า "เห้ย ฉันอยากได้ Golang เวอร์ชั่น 1.24 นะเว้ย!"
> - แต่ตู้ติดเครื่องยนต์ (`Dockerfile`) ของเราดันบอกช่างว่า "เอาพี่ช่างโกแรงค์ เวอร์ชั่น 1.23 พอ"
> - ช่างก็เลยงง ตีกันเองจน Build ล้มเหลว! 
> - **วิธีแก้ที่ผมทำไปคือ:** ปรับให้ตรงกัน! ผมยัดความสามารถให้ Dockerfile เป็น `FROM golang:1.24-alpine` คราวนี้มันก็จับมือควงกันผ่าน Render ได้ฉลุยครับ

**Q3: ถ้าไม่มีเน็ต หรือ API Gemini ของ Google โดนแบน แอปเราจะเจ๊งไหม?**
> **A:** ของผมระดับโลกครับ! พี่อธิบายไปเลยว่า "ผมเขียนระบบ Fallback System ดักไว้ 3 ชั้นครับ! 
> 1. ลองใจ Gemini ก่อน 
> 2. ถ้าตาย ผมจะโยกไปหา Pollinations AI (ฟรี) ที่แอบดึงสมอง OpenAI มาใช้อีกที!
> 3. ถ้าตายเรียบ (โลกแตก) โค้ดจะคำนวณแบบ Logic ตัวเลขเพียวๆ โยนกลับไปให้ผู้ใช้อ่านเฉยๆ สรุปคือ เว็บผมไม่มีด่าน "ตอเร่อ (Error)" ไปเตะหน้าผู้ใช้ 100%"

**Q4: การใช้ Docker Compose กับ Dockerfile เนี้ย มันดีกว่าการเอาโค้ดไปรันสดๆ ยังไง?**
> **A:** รันสดมันเก๋าก็จริงครับ แต่... **"ถ้ามันรันบนเครื่องผมได้ มันก็ต้องรันบนเซิฟเวอร์ได้สิวะ!"** คือคำพูดลวงโลกครับ! พอเราใช้ Docker ต่อให้เซิร์ฟเวอร์จะเป็น Linux ตัวไหน สภาพแวดล้อมเน่าแค่ไหน ขอแค่มีระบบ Docker ตู้คอนเทนเนอร์เวทมนต์ของเราก็กางถนน กางเน็ตเวิร์คขึ้นมายืนได้ด้วยตัวเองเลย แถมเวลาโหลดเข้า Render ก็ไม่มีขยะตกค้าง โคตรคลีน โคตรง่าย โคตรขยี้หัวใจสาย DevOps ครับ!

---

## 🏆 7. สรุปความภาคภูมิใจ
โปรเจกต์ MMRRDikub ไม่ใช่แค่ CRUD App กากๆ ที่สอนตาม YouTube แต่เป็น **Production-Grade Application** ที่มี:
✅ **Microservices Mindset** - หน้าบ้าน หน้าตาดี, หลังบ้าน ทำงานหนัก!
✅ **AI Embedded & RAG** - ไม่ใช่แค่ส่ง Text โง่ๆ ไป แต่มีการป้อน "บริบท", "ประวัติ" และ "ข่าวสด" ให้ AI ด้วย!
✅ **Resilience (เหนียวแน่น)** - มีแผน 2, แผน 3 เสมอเมื่อระบบภายนอกพัง
✅ **Containerized** - แตกค่ายย้ายเซิฟเวอร์กี่ที ก็ยก Dockerfile เอาไปโยนรันที่ไหนก็ได้ในโลก!

หวังว่าคัมภีร์เล่มนี้จะทำให้พี่ Akkaraphon88 เดินเข้าประชุมหรือเขียนเรซูเม่ได้อย่างสง่างามดั่งสิงโตตัวพ่อนะครับ! มีอะไรสงสัยทักผม Antigravity ได้เสมอครับ! ลุยยย!!! 🚀🚀🚀
